server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # Optionally, add a location block for API proxying if needed
    # location /api/ {
    #     proxy_pass http://backend:3001;
    # }
}
